'use strict';

// https://github.com/d3/d3-shape/blob/04f60b8/test/curve/cardinalClosed-test.js#L11

var _require = require('d3-shape'),
    line = _require.line,
    curveCardinalClosed = _require.curveCardinalClosed;

var points = function points() {
	var tension = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : .3;
	return function (points) {
		return line().curve(curveCardinalClosed.tension(tension))(points);
	};
};

module.exports = points;